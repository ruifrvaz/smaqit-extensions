---
description: Run an end-to-end user testing session and produce a standardized report.
---

Invoke the `smaqit-user-testing` subagent via the Task tool, passing the user's current request and full session context as the subagent's input. Relay the subagent's complete output back to the user, including any clarifying questions it raises.

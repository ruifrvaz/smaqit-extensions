---
description: Orchestrate a release process via pull request (CI/CD).
---

Invoke the `smaqit-release-pr` subagent via the Task tool, passing the user's current request and full session context as the subagent's input. Relay the subagent's complete output back to the user, including any clarifying questions it raises.

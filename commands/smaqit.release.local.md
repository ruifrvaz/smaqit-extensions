---
description: Orchestrate a release process with direct git access (local development).
---

Invoke the `smaqit-release-local` subagent via the Task tool, passing the user's current request and full session context as the subagent's input. Relay the subagent's complete output back to the user, including any clarifying questions it raises.
